<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Plano de fundo arredondado -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/greenAdd"/> <!-- Define a cor do plano de fundo -->
            <corners android:radius="15dp"/> <!-- Define o raio das bordas -->
        </shape>
    </item>
    <!-- Ícone vetorial -->
    <item android:drawable="@drawable/baseline_add_24"
        android:gravity="center"/>
</layer-list>
